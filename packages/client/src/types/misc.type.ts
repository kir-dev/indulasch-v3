export type Coordinates = {
  lat: number;
  lon: number;
};

export enum QueryKeys {
  WEATHER = 'weather',
  DEPARTURE = 'departure',
  MESSAGES = 'messages',
  BIKE = 'bike',
  SCHPINCER = 'schpincer',
  CONFIG = 'config',
  MAP = 'map',
}
