export type Coordinates = {
  lat: number;
  lon: number;
};

export enum QueryKeys {
  WEATHER = 'weather',
  DEPARTURE = 'departure',
  BIKE = 'bike',
  SCHPINCER = 'schpincer',
  CONFIG = 'config',
}
