import Head from 'next/head';

export function MainHead() {
  return (
    <Head>
      <title>🚌 | InduláSch</title>
      <meta
        name='description'
        content='Mobil alkalmazás mindennapi utazáshoz & távolról konfigurálható információs kijelző'
      />
    </Head>
  );
}
